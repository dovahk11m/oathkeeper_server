# 1117_도메인 확장과 데이터 초기화 작업

이 문서는 '그룹 단위 통계' 기능의 기반을 마련하기 위한 도메인 모델 확장 및 데이터 초기화 작업의 목표와 내용을 정의합니다.

---

### 1. 작업 정의

**"그룹 단위 통계 기능의 기반을 마련하기 위한 도메인 모델 확장 및 데이터 초기화 작업"**

이 작업은 단순히 테스트 데이터를 만드는 것을 넘어, 미래 기능을 위한 시스템의 구조를 개선하는 중요한 설계 변경 과정입니다.

---

### 2. 세부 작업 내용

#### **1) 도메인 모델 확장**

- **목표**: '그룹 스탯 누적' 기능이 데이터를 저장하고 조회할 수 있는 데이터베이스 구조(Schema)를 준비합니다.
- **수행 내용**:
    1.  **`Group.java` 수정**:
        -   그룹의 누적 성과를 기록할 필드를 추가합니다.
        -   예: `totalPlansCompleted`, `totalLateMinutes`, `totalTravelDistance` 등
    2.  **`Plan.java` 수정**:
        -   약속(`Plan`)이 어떤 그룹(`Group`)에 속하는지 명확히 알 수 있도록, `Group` 엔티티와의 연관 관계(`@ManyToOne`)를 설정하는 필드를 추가합니다.

#### **2) 데이터 초기화 로직 수정**

- **목표**: 확장된 도메인 모델 구조에 맞춰, 즉시 기능을 테스트하고 검증할 수 있는 일관된 테스트 환경을 구축합니다.
- **수행 내용**:
    1.  **`DataInitializer12_Plan.java` 수정**:
        -   새로운 `Plan` 샘플 데이터를 생성할 때, 위에서 연결된 `group` 필드에 기존에 생성된 '샘플 그룹'을 설정해주는 로직을 추가합니다.

---

### 3. 기대 효과

- **구조적 기반 마련**: 향후 개발될 '그룹 스탯 누적' 및 'AI 분석' 기능이 동작할 수 있는 안정적인 데이터 구조를 확보합니다.
- **테스트 효율성 증대**: 애플리케이션 실행 시, 기능 검증에 필요한 모든 데이터(Group-Plan-Participant)가 자동으로 생성되어, 수동 데이터 생성 없이 즉시 테스트가 가능해집니다.
