# 클라이언트 연동 지원 작업 계획

**작성일:** 2025-11-27
**담당:** 서버 개발팀
**목적:** Flutter 클라이언트의 AI 요약 및 실시간 지도 기능 연동 지원

---

## 📋 현재 상황

### ✅ 서버 측 완료 사항

1. **AI 요약 기능 개발 완료**

   - 플랜 요약: 약속 완료 시 자동 생성 (`POST /metrics/plan/{planId}/summary`)
   - 그룹 요약: 그룹 통계 기반 자동 생성 (`GET /api/groups/{groupId}/metrics/summary`)
   - 이벤트 체이닝 아키텍처로 안정성 확보

2. **실시간 지도 기능 준비 완료**

   - WebSocket 통신 구현
   - 참가자 위치 추적 API
   - 테스트용 API 제공

3. **API 문서 최신화**
   - 모든 주요 API 문서 2025-11-26 업데이트 완료

### 🎯 현재 단계

**클라이언트 연동 지원 대기 중** - 서버 개발 완료, 클라이언트 구현 시작 가능

---

## 📦 클라이언트에 제공할 API 문서

### 우선순위 1: AI 요약 기능 (필수)

#### 1. `METRIC_API.md` ⭐⭐⭐ **가장 중요**

- **내용:** AI 요약 폴링(Polling) 방식 구현 가이드
- **중요성:** 비동기 AI 요약 처리 방법의 핵심 가이드
- **포함 내용:**
  - 폴링 방식 전체 흐름
  - 3가지 응답 시나리오 (PENDING, COMPLETED, FAILED)
  - Flutter 예시 포함
  - 에러 처리 방법

#### 2. `GROUP_API.md` ⭐⭐⭐

- **내용:** 그룹/채팅 관련 모든 API (AI 요약 포함)
- **주요 엔드포인트:**
  - `GET /api/groups/{groupId}/metrics/summary` - 그룹 AI 요약 조회
  - `GET /api/groups/{groupId}/plans` - 그룹 내 약속 목록
  - `GET /api/groups` - 내 그룹 목록
- **특징:** 인증 필수, 페이징 지원

#### 3. `ERROR(에러응답)API.md` ⭐⭐

- **내용:** 공통 에러 응답 형식
- **중요성:** 모든 API 에러 처리 통일

### 우선순위 2: 실시간 지도 기능

#### 4. `LIVE_MAP_API.md` ⭐⭐⭐

- **내용:** WebSocket 기반 실시간 위치 공유
- **포함 내용:**
  - WebSocket 연결 방법
  - 위치 업데이트 수신
  - 테스트 API 사용법

### 참고용 (필요시 제공)

#### 5. `PLAN_API.md` ⭐

- **내용:** 약속 생성/수정/조회/삭제
- **제공 시점:** 약속 관리 기능 구현 시

#### 6. `MEMBER_API.md` ⭐

- **내용:** 회원 가입/로그인/프로필
- **제공 시점:** 이미 구현되어 있을 가능성 높음

---

## 🎯 작업 계획

### Phase 1: AI 요약 기능 연동 지원 (우선순위 1)

#### 1.1. 문서 전달 및 설명회

**일정:** 클라이언트팀 준비 완료 시
**방법:** 문서 공유 + 간단한 설명 (30분)

**전달할 문서:**

1. `METRIC_API.md` (핵심)
2. `GROUP_API.md`
3. `ERROR(에러응답)API.md`

**설명 포인트:**

- ✅ **폴링 방식 중요성:** AI 요약은 비동기 작업이므로 2~3초 간격 폴링 필수
- ✅ **3가지 상태 처리:** PENDING(로딩), COMPLETED(완료), FAILED(실패)
- ✅ **에러 처리:** 200 OK + FAILED vs 500 Error 구분

#### 1.2. 연동 지원 (On-demand)

**대응 항목:**

- [ ] API 호출 방법 질문 답변
- [ ] 응답 데이터 파싱 이슈 해결
- [ ] 폴링 로직 구현 검토
- [ ] 에러 케이스 처리 가이드

**예상 질문:**

1. "폴링 주기는 몇 초가 적당한가요?" → 2~3초 권장
2. "PENDING 상태가 계속되면 어떻게 하나요?" → 최대 30초 대기 후 타임아웃 처리 권장
3. "에러 메시지를 어떻게 표시하나요?" → `data.reason` 필드 사용

#### 1.3. 테스트 데이터 확인

**작업:**

- [ ] H2 DB 초기 데이터 확인 (`PlanScenario01_Completed.java`)
- [ ] 완료된 약속이 있는지 확인
- [ ] AI 요약이 정상 생성되는지 확인

**필요시 추가 데이터 생성 가능**

---

### Phase 2: 실시간 지도 기능 연동 지원 (우선순위 2)

#### 2.1. 문서 전달

**전달할 문서:**

1. `LIVE_MAP_API.md`

**설명 포인트:**

- ✅ WebSocket 연결 URL 및 인증 방법
- ✅ 위치 업데이트 메시지 형식
- ✅ 테스트 API 활용법

#### 2.2. WebSocket 연결 테스트

**작업:**

- [ ] 클라이언트 WebSocket 연결 성공 확인
- [ ] 메시지 송수신 테스트
- [ ] 연결 끊김/재연결 시나리오 검증

---

## 🛠️ 지원 도구 및 방법

### 1. 테스트 API 제공

**테스트용 강제 이동 상태 변경 API:**

```
POST /api/plans/{planId}/participants/{participantId}/test/force-movement-status
```

- 클라이언트가 실시간 지도 테스트 시 사용 가능

### 2. Swagger UI 활용

**URL:** `http://localhost:8080/swagger-ui.html`

- 클라이언트 개발자가 직접 API 테스트 가능
- 인증: `Authorization: Bearer {JWT}` 헤더 필요

### 3. 로그 모니터링

**확인 항목:**

- [ ] AI 서버 연동 로그 (`[metrics-push]`, `[plan-summary-fetch]`)
- [ ] WebSocket 연결/해제 로그
- [ ] 에러 발생 시 상세 로그 확인

---

## 📞 커뮤니케이션 채널

### 즉시 대응 (실시간)

**방법:** Slack/Discord 등 채팅
**대응 시간:** 업무 시간 내 즉시

**대응 항목:**

- API 호출 오류 (401, 404, 500 등)
- 응답 데이터 파싱 문제
- 로직 구현 질문

### 비동기 대응 (문서화)

**방법:** 이슈 트래커 또는 문서
**대응 시간:** 24시간 이내

**대응 항목:**

- API 스펙 변경 요청
- 새로운 기능 추가 요청
- 성능 개선 제안

---

## 📊 진행 상황 추적

### AI 요약 기능 (우선순위 1)

- [ ] **Step 1:** 문서 전달 및 설명 완료
- [ ] **Step 2:** 클라이언트 폴링 로직 구현 시작
- [ ] **Step 3:** 첫 번째 API 호출 성공
- [ ] **Step 4:** PENDING → COMPLETED 플로우 확인
- [ ] **Step 5:** 에러 처리 구현 완료
- [ ] **Step 6:** UI 표시 완성

### 실시간 지도 기능 (우선순위 2)

- [ ] **Step 1:** 문서 전달 완료
- [ ] **Step 2:** WebSocket 연결 성공
- [ ] **Step 3:** 위치 업데이트 수신 확인
- [ ] **Step 4:** 실시간 지도 UI 표시 완성

---

## 🚨 예상되는 이슈 및 대응

### 이슈 1: 폴링이 계속 PENDING 상태

**원인:**

- AI 서버 연결 실패
- 통계 데이터 부족
- Group에 완료된 Plan이 없음

**대응:**

1. 서버 로그 확인 (`[plan-summary-fetch]`)
2. AI 서버 상태 확인 (`http://192.168.0.3:8001/docs`)
3. 테스트 데이터 확인

### 이슈 2: 401 Unauthorized 에러

**원인:**

- JWT 토큰 누락 또는 만료
- Authorization 헤더 형식 오류

**대응:**

1. 헤더 형식 확인: `Authorization: Bearer {token}`
2. 토큰 유효성 확인
3. 로그인 재시도

### 이슈 3: WebSocket 연결 실패

**원인:**

- WebSocket URL 오류
- 인증 문제
- 방화벽/프록시 문제

**대응:**

1. URL 확인: `ws://localhost:8080/ws/...`
2. 인증 토큰 확인
3. 네트워크 설정 확인

---

## 📚 참고 자료

### 서버 측 핵심 파일

**AI 요약 기능:**

- `MetricsPushService.java` - AI 서버 통신
- `MetricsGroupService.java` - 그룹 요약 로직
- `PlanCompletionListener.java` - 이벤트 리스너

**실시간 지도:**

- `WebSocketConfig.java` - WebSocket 설정
- `PlanTestApiController.java` - 테스트 API

### 트러블슈팅 가이드

**ERROR 문서 (서버 개발팀 경험 공유):**

- `ERROR_1125_스웨거UI401에러.md` - 인증 문제 해결
- `ERROR_1117_AI서버연결실패.md` - 네트워크 이슈

---

## ✅ 체크리스트

### 준비 사항

- [x] 서버 AI 요약 기능 개발 완료
- [x] 서버 실시간 지도 기능 준비 완료
- [x] API 문서 최신화 완료
- [ ] 클라이언트팀에 문서 전달
- [ ] 설명회 일정 조율

### 연동 지원

- [ ] 첫 API 호출 성공 확인
- [ ] 폴링 로직 검토 완료
- [ ] 에러 처리 검증 완료
- [ ] WebSocket 연결 테스트 완료

### 최종 검증

- [ ] 클라이언트 AI 요약 UI 표시 확인
- [ ] 클라이언트 실시간 지도 동작 확인
- [ ] 성능 이슈 없음 확인
- [ ] 문서 피드백 반영 완료

---

## 🎯 성공 기준

1. ✅ **AI 요약 기능:** 클라이언트에서 그룹 요약을 정상적으로 조회하고 UI에 표시
2. ✅ **실시간 지도:** WebSocket으로 참가자 위치를 실시간으로 수신하고 지도에 표시
3. ✅ **에러 처리:** 모든 에러 케이스에 대해 사용자 친화적인 메시지 표시
4. ✅ **성능:** 폴링으로 인한 서버 부하 없음, WebSocket 안정성 확보

---

## 📅 다음 단계

1. **클라이언트팀과 일정 조율**
2. **문서 전달 및 설명회 진행**
3. **연동 과정에서 발생하는 이슈 즉시 대응**
4. **최종 검증 후 작업 완료**
