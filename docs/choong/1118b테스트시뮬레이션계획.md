# 1118b AI 서버 연동 테스트 시뮬레이션 계획

## 1. 목표

현재 학원 환경의 제약으로 인해 실제 AI 서버와 통신할 수 없는 상황에서, '플랜 완료' 시 통계 데이터를 AI 서버로 전송하는 기능의 구현부를 시뮬레이션하고, 데이터가 올바르게 준비되어 외부로 전송될 수 있는지 검증한다.

## 2. 구현 절차

### 2.1. `PlanCompletionListener`의 Push 로직 활성화

- **파일:** `PlanCompletionListener.java`
- **내용:**
    - 현재 주석 처리되어 있는 `pushService.pushPlan(planId);` 라인의 주석을 해제한다.
    - `PlanCompletedEvent`를 처리하는 `handlePlanCompletedEvent` 메서드의 가장 마지막 단계에서 `MetricsPushService`가 호출되도록 한다.

### 2.2. `MetricsPushService`에 Mocking 로직 구현

- **파일:** `MetricsPushService.java`
- **내용:**
    - `pushPlan(Long planId)` 메서드 내부에 실제 API를 호출하는 대신, 시뮬레이션 로직을 구현한다.
    - **내부 로직:**
        1. `planId`로 `Plan` 엔티티와 관련 통계 데이터(`ParticipantMetrics`)를 조회한다.
        2. AI 서버 API 명세에 맞는 DTO(Data Transfer Object)를 생성하고, 조회한 데이터로 DTO를 채운다.
        3. **(핵심)** 실제 `RestTemplate`이나 `WebClient`를 사용하여 외부 API를 호출하는 대신, 생성된 DTO 객체를 **JSON 형태로 변환하여 로그에 출력**한다.
        4. `log.info("[AI-PUSH-SIMULATION] Plan ID: {} / Payload: {}", planId, jsonPayload);` 와 같은 형태로 로그를 남겨, 어떤 데이터가 전송될 것인지 명확히 확인할 수 있도록 한다.

### 2.3. (선택) Mock API 컨트롤러 생성

- 만약 실제 HTTP 요청/응답 과정을 테스트하고 싶다면, `local` 프로파일에서만 활성화되는 임시 테스트용 컨트롤러를 만들 수 있다.
- **파일:** `AiMockApiController.java`
- **내용:**
    - `@Profile("local")` 어노테이션을 사용하여 `local` 환경에서만 동작하도록 설정한다.
    - AI 서버의 API 엔드포인트와 동일한 경로(`@PostMapping("/ai/plan-summary")` 등)를 가진 메서드를 생성한다.
    - 이 메서드는 요청으로 받은 JSON 데이터를 그대로 로그에 출력하고, 간단한 성공 메시지(`{"status": "success"}`)를 응답으로 반환한다.
    - `MetricsPushService`에서는 이 Mock API 컨트롤러의 주소로 HTTP 요청을 보내도록 설정한다.

## 3. 검증 방법

1. `DataInitializer`를 통해 생성된 테스트 플랜(ID: 2)의 마지막 참가자를 도착시켜 플랜을 `COMPLETED` 상태로 만든다.
2. 서버 로그를 확인한다.
3. `[AI-PUSH-SIMULATION]` 이라는 키워드가 포함된 로그가 출력되는지 확인한다.
4. 로그에 출력된 JSON Payload의 내용이, 완료된 플랜(ID: 2)의 통계 데이터와 일치하는지 검증한다.

## 4. 기대 효과

- 실제 AI 서버 없이도 데이터 전송 로직의 구현과 검증을 완료할 수 있다.
- 향후 실제 서버 환경으로 전환 시, `MetricsPushService`의 HTTP 클라이언트 부분만 실제 AI 서버 주소로 변경하면 되므로 전환 비용을 최소화할 수 있다.
