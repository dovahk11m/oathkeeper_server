# 1126 작업 계획 (TODO)

- **작성일:** 2025-11-26
- **어제 작업 요약:** 그룹 AI 요약 기능의 401 에러 트러블슈팅 및 해결, 관련 리팩토링 진행.
- **주요 목표:** 이관되었던 **약속 중간 저장 문제**와 **실시간 지도 기능 클라이언트 연동** 작업을 다시 시작하여 완료한다.

---

## 1. 최우선 과제

### 1.1. 긴급 점검: 약속 중간 저장 문제 해결 (재개)

- **목표:** `Plan`, `Participant` 등 도메인 변경으로 인해 발생한 약속 중간 저장 문제를 해결한다.
- **세부 과업:**
    - [ ] **서버-클라이언트 데이터 구조 비교:** 클라이언트에서 약속 생성/수정 시 전송하는 데이터(`DTO`)가 현재 서버의 `PlanRequest` DTO와 일치하는지 다시 한번 확인한다.
    - [ ] **핵심 로직 디버깅:** `PlanFacade`의 `createPlan`, `updatePlan` 메서드와 `PlanService`의 관련 로직에 브레이크포인트를 설정하고, 클라이언트 요청 시 데이터가 어떻게 변환되고 저장되는지 단계별로 추적한다.
    - [ ] **상태 관리 확인:** 특히 `MovementStatus`, `ParticipantStatus` 등 Enum 타입의 필드들이 정상적으로 처리되는지 집중적으로 확인한다.

## 2. 다음 과제

### 2.1. 실시간 지도 기능 클라이언트 연동 지원 및 테스트 (재개)

- **목표:** 클라이언트 개발자와의 협업을 통해 실시간 지도 기능의 클라이언트 연동을 완료하고, 통합 테스트 환경을 구축한다.
- **세부 과업:**
    - [ ] **클라이언트 연동 현황 파악:** 클라이언트 개발자와 소통하여 `LIVE_MAP_API.md` 기반의 기능 구현이 어디까지 진행되었는지 확인하고, 현재 겪고 있는 어려움이 있는지 파악한다.
    - [ ] **테스트용 강제 이벤트 API 구현:**
        - [ ] `PlanTestApiController`에 특정 참가자의 "도착"(`force-arrived`) 및 "정체"(`force-stationary`) 이벤트를 강제로 트리거하는 테스트용 API를 우선적으로 구현하여 클라이언트 측의 테스트를 지원한다.
    - [ ] **통합 테스트 준비:** `DataInitializer12_Plan`의 '진행중인 약속' 시나리오(Plan ID: 2, 3)를 기반으로, Postman 또는 `curl`을 사용하여 도착/정체 이벤트 발생 시나리오를 미리 테스트하고 예상 결과를 정리한다.

## 3. 기타

- [ ] 어제 진행한 리팩토링(HttpClient Bean 등록, DTO 네이밍 컨벤션 수정 등) 이후, 그룹 AI 요약 기능이 여전히 정상적으로 동작하는지 회귀 테스트를 통해 최종 확인한다.
