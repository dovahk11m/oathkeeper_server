# 1121 작업 계획 (TODO) 및 결산

- **작성일:** 2025-11-20
- **주요 목표:** AI 서버 연동 기능의 전체 파이프라인을 완성하고, 안정성을 높여 운영 가능한 수준으로 고도화한다.

---

## 1. 개인 작업 계획 (충희)

### 1.1. 단기 계획 (11/21)

-   **오전:** AI 요약 보고서 제공 API 구현 (`GET /api/plans/{planId}/summary`)
-   **오후:** 폴링(Polling) 방식으로 AI 결과 수신 로직 고도화
-   **자투리 시간:**
    -   비밀번호 찾기 기능 동작 여부 확인 및 필요시 수정

---

## 2. 작업 결산 (11/24)

-   **AI 요약 기능 고도화:** 비동기 폴링 방식 및 `SummaryStatus` Enum을 도입하여 안정적인 AI 요약 파이프라인을 구축.
-   **실시간 지도 기능 개선:**
    -   `MovementStatus` Enum을 도입하여 참가자의 실시간 이동 상태(정체, 도착 등)를 명확히 관리.
    -   GPS 미변화, 자동 도착 감지 로직 및 관련 WebSocket 알림 기능을 구현.
    -   위치 좌표 필드명(`lat`, `lng`)을 통일하고, `TrackService`의 DB 호출을 최적화하여 성능을 개선.
-   **기타 개선 및 문서화:**
    -   `DataInitializer`에 다양한 테스트 시나리오를 추가하고, `Tag` 도메인에 `imageUrl` 필드를 추가.
    -   팀 회의록 및 장기 계획 문서를 분리/정리하고, `1124_todo.md`에 단기 계획을 반영.
